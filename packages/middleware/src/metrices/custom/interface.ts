// import { MetricsManager } from '..';
// import { config } from '../../../config/config';
// import { METRIC_PREFIX } from '../consts';

// export abstract class CustomMetrics {
//   abstract registerMetrics(register: { registerMetric: (metric: any) => void }): void;

//   public name(n: string) {
//     return `${METRIC_PREFIX}_${n}`;
//   }
// }
