// Re-export the new clean AgentCard component
export { AgentCard as default } from './AgentCard';

// Export types for external use if needed
export type { AgentCardCallbacks, AgentPermissions, IAgent } from './types';

