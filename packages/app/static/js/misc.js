window.addEventListener('DOMContentLoaded', (event) => {
  // if (document.location.pathname.startsWith('/builder') || document.location.pathname.startsWith('/partners')) return;
  // const banner = document.createElement('div');
  // banner.className = 'w-full flex flex-wrap items-center justify-start mx-auto p-2 bg-primary-100 text-white font-semibold italic';
  // banner.innerHTML =
  //     '<div>Announcement: Love SmythOS? Help us spread the word and get free account upgrades. <a class="underline" href="/partners">Learn More</a>.</div>';
  // const nav = document.querySelector('nav');
  // if (nav) nav.prepend(banner);
});
