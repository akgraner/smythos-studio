# Donot delete this file
# This folder is used to store the lambda archives
# The lambda archives are used to deploy the lambda functions
# The lambda archives are zipped and sent to the aws lambda function
# The lambda archives are unzipped and the code is deployed
