<div class="<%= pos=='right' ? 'col-start-6 col-end-13' : 'col-start-1 col-end-8' %> p-3 rounded-lg">
    <div class="flex flex-row items-center <%= pos=='right' ? 'justify-start flex-row-reverse':'' %>">
        <div class="flex items-center justify-center h-10 w-10 rounded-full bg-indigo-500 flex-shrink-0">
            <%= user.shortname %>
        </div>
        <div class="relative <%= pos=='right' ? 'mr-3' : 'ml-3' %> text-sm bg-white py-2 px-4 shadow rounded-xl">
            <div><%= message %></div>
        </div>
    </div>
</div>